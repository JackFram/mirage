[build-system]
requires = ["setuptools", "wheel", "packaging"]
build-backend = "setuptools.build_meta"

[project]
name = "mpkmoe"
version = "0.0.1"

dependencies = [
    nvidia-cutlass-dsl,
    tg4perfetto==0.0.4,
]

[tool.setuptools.packages.find]
where = ["."]